# 代理节点体检维护规则

本文件适用于“代理节点体检 / Proxy uptime monitor”项目。修改前先阅读 `README.md`、
`README_EN.md`、`SECURITY.md` 与 `docs/发布审计.md`。

## 产品与兼容性

- 同时维护 Windows/macOS/Linux 本地运行入口，以及带 `systemd` 的 Linux
  服务器部署路径；不能用仅适配单一环境的实现替代另一条路径。
- 保持依赖克制、SQLite 数据兼容、现有 API 与订阅解析行为兼容。涉及数据库
  结构时必须提供可回滚的迁移并覆盖测试。
- 管理界面必须保留完整的响应式能力、键盘可用性、四套全局主题和右上角主题
  选择器。深色主题使用深灰而非纯黑，主题选择必须持久化。
- 品牌图标必须同时维护 SVG 与 ICO；README 首屏和 GitHub Social preview
  使用 1280×640、实色背景且小于 1 MB 的 PNG。

## 安全与发布门禁

- 不提交 `.env`、数据库、日志、订阅原文、节点真实地址、部署身份、私有域名、
  内网主机、真实账号或任何凭据。示例只使用 `localhost`、保留示例域名和明显
  的合成值。
- 所有外部命令都使用参数数组且禁用 shell；所有 SQL 动态片段只允许固定白名单
  或参数占位符，用户值必须绑定传入。
- 发布前至少执行：`python -m compileall -q app scripts tests`、`pytest -q`、
  依赖漏洞审计、两类秘密扫描、静态安全扫描和代表性桌面/移动端渲染检查。
- 静态工具的误报必须逐项确认并记录依据，不能靠整类禁用或忽略文件掩盖。
- 修改公开版本时同步中英文 README、预览图、`CHANGELOG.md`、工作区根目录
  `README.md` 与 GitHub 个人主页仓库的 `README.md`。

