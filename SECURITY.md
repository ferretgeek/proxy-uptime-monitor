# 安全策略

## 支持范围

安全修复优先合入当前最新版本。部署者应使用最新源码和最新数据库迁移；旧版
可能不再单独提供补丁。

## 报告安全问题

请通过代码托管平台提供的私密安全报告功能联系维护者。仓库地址确定后，可在
这里补充专用安全邮箱。请不要在公开 Issue、讨论区或日志附件中发布：

- 可直接利用的漏洞步骤或攻击载荷；
- 真实订阅地址、节点 URI、Webhook、Cookie、数据库或备份；
- 管理员密码、`AIRPORT_ENCRYPTION_KEY`、`AIRPORT_SESSION_PEPPER`；
- 服务器地址、SSH 凭据或包含上述信息的截图。

报告应尽量包含受影响版本、影响范围、最小复现条件和建议修复方向。维护者确认
问题后再协调公开时间。

## 部署者安全清单

- 仅监听受信任的局域网地址，不要把管理面板直接暴露到公网；
- 保持 `/etc/airport-monitor/env`、数据库和备份为受限权限；
- 只使用本项目 `backup.sh` / `restore.sh` 生成和恢复的允许清单备份；维护脚本把
  环境文件当作数据解析，部署者不应绕过解析器用 shell 载入该文件；
- 不复用管理员密码，怀疑泄露时立即轮换凭据和加密密钥；
- 更新前创建备份，并通过健康接口和数据库完整性检查验收；
- 不把 `.env`、数据库、日志、截图、发布包或本地凭据提交到仓库；
- Webhook 接收端同样应进行鉴权、限速和日志脱敏。

## 数据与外部请求

程序会按配置通过代理节点访问启用的检测目标，并可查询公开的出口地区服务。
部署者应确认此类请求符合所在地法律、网络服务条款及组织政策。项目不提供真实
订阅或代理节点，也不对第三方服务的可用性作保证。
